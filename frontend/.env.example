# Base Sepolia Network Configuration
# Default network is Base Sepolia, no env variables needed for basic setup

# Optional: Add your own project ID from wagmi for analytics
# NEXT_PUBLIC_PROJECT_ID=your_project_id_here
